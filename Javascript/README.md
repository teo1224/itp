#Javascript

First, I defined the variables f and c by using ```let f = 99; and let c = (formula);```. Then, I used ``console.log`` to display both the Fahrenheit and Celsius values, connecting them to f and c respectively.